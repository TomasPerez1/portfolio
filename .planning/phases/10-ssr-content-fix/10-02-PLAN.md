---
phase: 10-ssr-content-fix
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - my-app/src/app/i18n/client.ts
  - my-app/src/app/i18n/index.ts
  - my-app/src/app/i18n/__assert.ts
  - my-app/src/app/ui/LangLoader.tsx
  - my-app/package.json
  - my-app/package-lock.json
autonomous: false

requirements: [SSR-01, SSR-03]

must_haves:
  truths:
    - "next build && next start completes cleanly; the browser console shows zero hydration errors/warnings on both /en and /es"
    - "View-source / curl on /en and /es shows full rendered portfolio content in the initial HTML — no spinner-only response"
    - "package.json no longer lists i18next, react-i18next, or i18next-resources-to-backend; all content renders identically to before in both locales"
    - "Zero source references remain to the deleted i18n modules (i18n/client, i18n/index, LangLoader, i18next, react-i18next)"
  artifacts:
    - path: "my-app/package.json"
      provides: "Dependency manifest with the three i18next packages removed"
  key_links:
    - from: "curl http://localhost:3000/en"
      to: "rendered portfolio content (Hero/About/Footer text)"
      via: "SSR initial HTML"
      pattern: "Tomás"
    - from: "curl http://localhost:3000/en"
      to: "absence of spinner markup"
      via: "SSR initial HTML"
      pattern: "animate-spin"
---

<objective>
Close out the SSR fix: confirm via a grep gate that no source file still imports the i18next chain, then delete the four now-dead modules, uninstall the three i18next packages, and validate the running production build for crawlable content and zero hydration errors on both locales.

This plan MUST run after Plan 01 (10-01). Deleting the i18n modules before the consumers were migrated would break the build (Pitfall 3, 10-RESEARCH.md) — that is why the grep gate is the hard entry condition here. By the time this plan runs, Plan 01 has already removed every source reference to i18next; this plan verifies that, then removes the dead code and the packages.

Purpose: Complete SSR-01 (runtime confirmation of crawlable content) and SSR-03 (i18next + LangLoader removed, zero translation loss, zero hydration errors).
Output: Four files deleted (i18n/client.ts, i18n/index.ts, i18n/__assert.ts, ui/LangLoader.tsx); three packages uninstalled; validated production build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-ssr-content-fix/10-RESEARCH.md
@.planning/phases/10-ssr-content-fix/10-PATTERNS.md
@.planning/phases/10-ssr-content-fix/10-VALIDATION.md
@.planning/phases/10-ssr-content-fix/10-01-SUMMARY.md

<interfaces>
<!-- Deletion targets confirmed present in the codebase. Pre-deletion grep gate is mandatory. -->

Files to DELETE (all confirmed to exist; none has any source importer after Plan 01):
- my-app/src/app/i18n/client.ts        — i18next useTranslation wrapper, superseded by usePortfolioData/getPortfolioData
- my-app/src/app/i18n/index.ts         — i18next instance initializer, no longer needed
- my-app/src/app/i18n/__assert.ts      — type-assertion-only file (en/es as unknown as PortfolioData, void _en/_es); does NOT import client.ts/index.ts; role fully subsumed by getPortfolioData.ts's identical assertion (10-RESEARCH.md Pattern 4)
- my-app/src/app/ui/LangLoader.tsx     — spinner component, unreferenced after the ClientPage gate removal in Plan 01

DO NOT delete my-app/src/app/i18n-config.ts — it defines i18n.locales/defaultLocale/fallbackLng, is consumed by app/[lang]/page.tsx's generateStaticParams, and is unrelated to the i18next runtime being removed.

npm uninstall command (the only dependency op in this phase):
```bash
npm uninstall i18next react-i18next i18next-resources-to-backend
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep gate, then delete the four dead i18n modules</name>
  <read_first>
    - my-app/src/app/i18n/__assert.ts (confirm it imports only the two JSON files + PortfolioData type — NOT client.ts/index.ts — before deleting, per the STATE.md pre-check)
    - .planning/phases/10-ssr-content-fix/10-01-SUMMARY.md (confirm Plan 01 migrated Hero/Footer/ClientPage)
  </read_first>
  <action>
    First, run the grep gate (hard precondition — this is Pitfall 3's safeguard from 10-RESEARCH.md). Search all source for any remaining reference to the i18next chain:
    `rg -n "i18n/client|i18n/index|LangLoader|from \"i18next\"|from \"react-i18next\"" my-app/src`
    The expected result is ZERO matches. If ANY match is found, STOP — do not delete anything; the migration in Plan 01 is incomplete and must be fixed first (a deletion now would cause `Module not found` build failures).

    Only after the grep gate returns zero matches, delete these four files: my-app/src/app/i18n/client.ts, my-app/src/app/i18n/index.ts, my-app/src/app/i18n/__assert.ts, and my-app/src/app/ui/LangLoader.tsx. Before deleting __assert.ts, confirm by reading it that it imports only `../../../public/locales/{en,es}/common.json` and the `PortfolioData` type (no import from client.ts/index.ts) — its type-check role is subsumed by getPortfolioData.ts. Do NOT delete i18n-config.ts (it is unrelated to the i18next runtime and is still consumed by generateStaticParams).
  </action>
  <acceptance_criteria>
    - The grep gate `rg -n "i18n/client|i18n/index|LangLoader|from \"i18next\"|from \"react-i18next\"" my-app/src` returns zero matches BEFORE any deletion
    - my-app/src/app/i18n/client.ts does not exist
    - my-app/src/app/i18n/index.ts does not exist
    - my-app/src/app/i18n/__assert.ts does not exist
    - my-app/src/app/ui/LangLoader.tsx does not exist
    - my-app/src/app/i18n-config.ts STILL exists (was not deleted)
    - my-app/src/app/i18n/getPortfolioData.ts and usePortfolioData.ts STILL exist
  </acceptance_criteria>
  <verify>
    <automated>cd my-app && npm run build</automated>
  </verify>
  <done>Grep gate confirmed zero source references; the four dead modules are deleted; i18n-config.ts and the two data accessors remain; `npm run build` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Uninstall the three i18next packages</name>
  <read_first>
    - my-app/package.json (the dependencies block — confirm i18next, react-i18next, i18next-resources-to-backend are listed)
  </read_first>
  <action>
    From the my-app directory, run exactly: `npm uninstall i18next react-i18next i18next-resources-to-backend`. This removes all three from package.json `dependencies` and updates package-lock.json. This is the ONLY dependency operation in this phase — no package is installed (do NOT add any test framework; Playwright is deferred per STATE.md and the milestone guidance is "only remove dependencies"). After it completes, confirm via `npm ls i18next react-i18next i18next-resources-to-backend` that all three report "not found" / empty, and that the package.json dependencies block no longer lists any of them.
  </action>
  <acceptance_criteria>
    - `rg -c "i18next" my-app/package.json` returns 0 (no i18next, react-i18next, or i18next-resources-to-backend entry remains)
    - `npm ls i18next react-i18next i18next-resources-to-backend` (run in my-app) reports all three as not found / empty
  </acceptance_criteria>
  <verify>
    <automated>cd my-app && npm run build</automated>
  </verify>
  <done>The three i18next packages are removed from package.json and node_modules; `npm run build` exits 0 with no i18next dependency present.</done>
</task>

<task type="auto">
  <name>Task 3: SSR content + dependency-removal automated checks on the production build</name>
  <read_first>
    - .planning/phases/10-ssr-content-fix/10-VALIDATION.md (the Per-Task Verification Map — the six requirement checks)
  </read_first>
  <action>
    Produce a production build and serve it, then run the automated half of the 10-VALIDATION.md requirement checks (the curl/grep checks for SSR-01 and SSR-03). Start the production server in the background (`cd my-app && npm run build && npm run start`, default port 3000), wait until it responds, then run the curl checks below against the running server. Stop the server when the checks complete.

    SSR-01 content presence (initial HTML, no JS):
    - `curl -s http://localhost:3000/en` must contain "Tomás" (count > 0).
    - `curl -s http://localhost:3000/es` must contain a Spanish-only string from the about/section copy (e.g. "Sobre mí") (count > 0).
    SSR-01 spinner absence + Hero presence:
    - `curl -s http://localhost:3000/en | grep -c "animate-spin"` must equal 0 (the LangLoader spinner markup is gone).
    - `curl -s http://localhost:3000/en` must contain "Voxel · click to shuffle" (count > 0 — confirms the Hero rendered into server HTML).
    SSR-03 dependency + reference audit:
    - `rg -c "i18next" my-app/package.json` equals 0.
    - `rg -n "i18n/client|i18n/index|LangLoader|from \"i18next\"|from \"react-i18next\"" my-app/src` returns zero matches.

    Record each result. Do NOT add any test framework to automate these — curl/grep against the running build is the documented method (10-VALIDATION.md Wave 0).
  </action>
  <acceptance_criteria>
    - `curl -s http://localhost:3000/en | grep -c "Tomás"` > 0
    - `curl -s http://localhost:3000/es | grep -c "Sobre mí"` > 0
    - `curl -s http://localhost:3000/en | grep -c "animate-spin"` == 0
    - `curl -s http://localhost:3000/en | grep -c "Voxel · click to shuffle"` > 0
    - `rg -c "i18next" my-app/package.json` == 0
    - `rg -n "i18n/client|i18n/index|LangLoader|from \"i18next\"|from \"react-i18next\"" my-app/src` returns zero matches
  </acceptance_criteria>
  <verify>
    <automated>cd my-app && npm run build</automated>
  </verify>
  <done>The production build serves full crawlable content (Tomás/Spanish copy/Hero present) with zero spinner markup on both locales, and the i18next dependency + source-reference audits are clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01 removed the i18next async spinner gate and migrated content reads to the static getPortfolioData/usePortfolioData accessors; this plan deleted the four dead i18n modules and uninstalled the three i18next packages. Task 3 confirmed (via curl) that the production server renders full content with no spinner markup on both locales. The one thing curl CANNOT confirm is the React hydration step — whether the client-side tree matches the SSR HTML once JS runs (Pitfall 1, 10-RESEARCH.md: removing the gate diffs the real tree against SSR output for the first time, which can surface latent hydration mismatches in theme-branching / voxel components).
  </what-built>
  <how-to-verify>
    1. With the production build running (`cd my-app && npm run build && npm run start`), open `http://localhost:3000/en` in a browser with DevTools open (Console tab).
    2. Confirm the Console shows NO "Hydration failed because the server rendered HTML didn't match the client" and NO "Text content does not match server-rendered HTML" warnings.
    3. Confirm the page renders the full portfolio (Nav, Hero voxel, About, Featured, Stack, Experience, Contact, Footer) — not a spinner — and the CV download button points to a real `/cv/FULLSTACK-TOMAS_PEREZ_en.pdf` link.
    4. Repeat steps 1-3 for `http://localhost:3000/es` (Spanish copy throughout; CV link `/cv/FULLSTACK-TOMAS_PEREZ_es.pdf`).
    5. Confirm content parity: both locales display the same content they did before this phase (no missing/blank sections, no English text leaking into /es or vice versa).
    Expected outcome: clean console on both locales, full content rendered, correct per-locale CV links.
  </how-to-verify>
  <resume-signal>Type "approved" if the console is clean and content renders correctly on both /en and /es, or describe any hydration warning or missing content observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package manager → dependency tree | `npm uninstall` removes three packages — it reduces (never adds) installed code |

## STRIDE Threat Register

This plan only DELETES code and REMOVES packages. It introduces no new input handling, auth, session, network, or cryptography. The spinner removal (completed in Plan 01) exposes already-public, build-time static content. No new attack surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Denial of Service | Deleting i18n modules out of order | mitigate | The grep gate in Task 1 is a hard precondition — deletion only proceeds when `rg` confirms zero remaining source references, preventing a `Module not found` broken build (Pitfall 3). |
| T-10-SC | Tampering | npm dependency operation (supply chain) | accept | The only dependency op is `npm uninstall` of three already-trusted, already-installed packages — this strictly REDUCES supply-chain surface. No package is installed; the Package Legitimacy Gate (slopcheck / registry verification) applies to new installs and is not applicable. |
| T-10-04 | Information Disclosure | Full content now in initial SSR HTML | accept | Intended goal (SSR-01). `common.json` is public, build-time marketing/portfolio content — no secrets or PII. |
</threat_model>

<verification>
Per 10-VALIDATION.md: `cd my-app && npm run build` after each task commit; `npm run build && npm run start` + manual browser console check on /en and /es before `/gsd:verify-work`.

Requirement coverage in this plan:
- SSR-01: Task 3 curl checks (content present, spinner absent, Hero rendered) on both locales.
- SSR-03 (deps): Task 2 `npm uninstall` + Task 3 `rg -c "i18next" package.json == 0` and the source-reference grep.
- SSR-03 (hydration): the blocking human-verify checkpoint (Task 4) — the only check that cannot be automated without Playwright (deferred). This is the documented sufficient method per 10-VALIDATION.md Manual-Only Verifications.
</verification>

<success_criteria>
- Grep gate returned zero source references to the i18next chain before any deletion.
- i18n/client.ts, i18n/index.ts, i18n/__assert.ts, ui/LangLoader.tsx are deleted; i18n-config.ts and the two data accessors remain.
- package.json no longer lists i18next, react-i18next, or i18next-resources-to-backend; `npm ls` reports all three not found.
- `npm run build` exits 0 after every task.
- curl on /en and /es shows full content, zero "animate-spin" markup, Hero present.
- Human-verified: browser console is clean (no hydration warnings) and content renders identically on both locales, with correct per-locale CV links.
</success_criteria>

<output>
Create `.planning/phases/10-ssr-content-fix/10-02-SUMMARY.md` when done.
</output>
